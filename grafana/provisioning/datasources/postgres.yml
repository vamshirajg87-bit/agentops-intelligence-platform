apiVersion: 1

datasources:
  - name: AgentOps PostgreSQL
    type: postgres
    url: postgres:5432
    database: agentops
    user: grafana_reader
    jsonData:
      sslmode: disable
      postgresVersion: 1700
    secureJsonData:
      password: $__env{GF_DATASOURCE_AGENTOPS_PASSWORD}
